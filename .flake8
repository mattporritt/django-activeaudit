[flake8]
ignore = D203
max-line-length = 132
exclude =
    # No need to traverse our git directory
    .git,
    # There's no value in checking cache directories
    __pycache__,
    # leave virtual env alone
    venv/
max-complexity = 10
